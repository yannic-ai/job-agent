# 第 2 章开发过程

## brainstorm 定稿（2026-09-10）

- **用户关键原话：** 「给现有的简历系统简历建立主流程」；角色协作选 C（评估专家独立，解析完成后先评估再决策）；入口选 A（`match <jd.md> <resume.md>`）；JD schema 选 A（精简匹配集）；简历来源先说「解析异步落向量库，这里直接读取向量库」，随后改选 C（本章仍走第 1 章 `parse_resume`，向量库只预留接口）；tool 协作定为 B 并加码「每个专家有自己的 tool list，每个节点 tool call 次数限制 2 次」；评估维度选 B（技能/年限/学历/地点/职责五维并行节点）；JD 样例直接粘贴 Xagent Agent 平台岗位；决策选 A（五维算术平均 + 阈值）；分制选 B（每维 1–5，≥4 推荐，3–<4 待定，<3 不推荐）；编排选方案 1（StateGraph 扇出/扇入 + 短 tool 循环）。架构/模块/Prompt 三节均回复「可以」。
- **关键产出：** 设计 spec `docs/superpowers/specs/2026-09-10-resume-matching-pipeline-design.md`。范围锁定为第 2 章主流程；需求解析做细；其余专家做薄；无开放 Agent Loop、无 RAG。
- **拒绝或纠偏：** 否决「本章读真实向量库」（与「本章不做向量检索、RAG」冲突，用户改选预留接口）；否决 Supervisor；否决评估/决策纯 Python 无 tool list；否决硬门槛一票否决；否决评估单节点一次打完五维。用户用真实 JD 原文收窄了岗位样例，地点缺失明确为 `null`。
- **翻车与返工：** 无。过程中一度把「读向量库」当成简历提取方案，已在提问中拦住，未写入错误 spec。

## 计划评审通过（2026-09-10）

- **用户关键原话：** 「执行」（对 spec 评审的回复，视为批准 spec 并继续 Superpowers 下一步）。
- **关键产出：** 实现计划 `docs/superpowers/plans/2026-09-10-resume-matching-pipeline.md`。Context7 已核对：`StateGraph` 并行边汇入 join 节点会等待全部完成；`from langchain.tools import tool`；`bind_tools` / `tool_calls`；`with_structured_output(..., method="json_mode")`；`await graph.ainvoke`。本机 langchain 1.3.x，计划锁定 `langgraph>=1.0`。
- **拒绝或纠偏：** 无新的方案否决。计划按已锁定方案 1 拆成 9 个任务；Prompt/JD 提取/全图用评估集，确定性切分/打分/阈值/runtime/图拓扑走 TDD。
- **翻车与返工：** 无。用户未单独再审计划正文，用「执行」跳过二次确认。

## Task 1 完成（2026-09-10）

- **用户关键原话：** 「1，双层循环，外层循环控制阶段推进、预算心跳、检查点恢复、契约验收与失败收口，内层循环执行具体操作」
- **关键产出：** `JobRequirement` / `DimensionScore` / `Decision` / `MatchingFileError`；commits `33a839a`、`2de26bf`、`56a8bd3`。评审门禁补了 score 1–5 与 evidence 非空。
- **拒绝或纠偏：** 否决「Decision/DimensionScore 标量默认 null」（与产出对象契约冲突）；否决 evidence 必须正则校验汉字（中文是 Prompt 约束，schema 只拒空串）。
- **翻车与返工：** 两轮 fix：漏测 score 范围；漏 evidence 非空。第三轮评审因「必须中文字符」未再改 schema。

## Task 2 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** `load_jd` / `split_jd_sections` / `format_jd_sections`；commits `2361e1a`、`61fb115`。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 评审指出 UTF-8 失败未包成 `MatchingFileError`、标题行进了 body 导致重复标题；已修。Minor：缺「（无）」与 `###` 留块测试，留给终审。

## Task 3 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** `scoring.py` / `decision.py` / `report.py`；commit `625e22a`。
- **拒绝或纠偏：** brief 用例 `[2,3,3,3,3]==待定` 与阈值冲突（均值 2.8），按 spec 改为恰好 3.0 的待定用例。
- **翻车与返工：** 无。

## Task 4 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** `run_expert`；commits `545cbee`、`db35afe`。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 初版按「模型轮次」计数；评审要求按实际 tool 执行次数截断，已修。

## Task 5 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** `state.py` / stub `nodes.py` / `graph.py`；`langgraph>=1.0`；commit `7a52ef3`。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。Minor：图测试未断言 `parse_join` 与边。

## Task 6 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** JD Prompt / `read_and_split_jd` / `jd_parse_node`；commit `c7fed79`。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。

## Task 7 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** 简历/五维评估/决策/报告专家；commits `9b0d5f3`、`9020c07`。
- **拒绝或纠偏：** 无。
- **翻车与返工：** `resume_extract_node` 先调 tool 再 `run_expert` 可能超过 2 次 tool call；已改为 Python `parse_resume` 写状态。Minor：简历可能解析两遍。

## Task 8 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** `run_matching` 与 `python -m job_agent.match`；commit `80ea741`。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。

## Task 9 完成（2026-09-10）

- **用户关键原话：** （无新指令，外层继续推进）
- **关键产出：** `tests/fixtures/xagent_jd.md`、gold、live 评估；commits `5942a74`、`4fa3c1a`。live：`eval_jd_parse` 与 `eval_matching_pipeline` 2 passed。
- **拒绝或纠偏：** 否决「fixture 里 AlAgent/A| 是损坏应改原文」——那是用户粘贴的 JD；spec 要求提取时归一，不改样例。
- **翻车与返工：** 初跑 live：must_have 抽空 python；dimension 输出「地点」校验失败。已做维名 coerce 与 Prompt 收紧后重跑通过。

## code review 结论（2026-09-10）

- **用户关键原话：** （无单独审查指令；按 Superpowers 全部分支终审）
- **关键产出：** 终审 Ready to merge: With fixes。已修：简历只解析一次；图测试锁定 `parse_join` 与扇入边；从索引拿掉误提交的 SDD 报告。commit `d38711a`。默认 `python -m pytest` **62 passed**；live 评估此前 **2 passed**。
- **拒绝或纠偏：** 否决改写用户 JD fixture 中的 OCR 笔误。Minor（docstring、`（无）`/`###` 测、截断 tool_calls 的 history）未再改，留给后续。
- **翻车与返工：** 见 Task 4/7/9。

## finish（2026-09-10）

- **用户关键原话：** 「1，双层循环…」作为执行方式；实现完成后等待用户选合并/PR/保留分支。
- **关键产出：** 演示命令 `python -m job_agent.match tests/fixtures/xagent_jd.md "/Users/yannic/面试/宗艳云简历v9.md"`；过程笔记本文件。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 见上。
