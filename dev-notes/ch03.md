# 第 3 章开发过程

## brainstorm 定稿（2026-09-10）

- **用户关键原话：** 「我要讲简历提取后存入向量库，结构该如何定义」；切块选 B；落库讨论后选匹配范围 B、入口 C、简历 ID 选 A、重建选 B、评估接检索选 A、空召回选 C；主流程 / 切块表结构 / 模块与图 / 失败验收四段均回复「可以」。中途纠正「不要直接写文档，目前还存在讨论需求阶段」，已删提前写下的 spec。
- **关键产出：** 设计 spec `docs/superpowers/specs/2026-09-10-resume-knowledge-base-design.md`。
- **拒绝或纠偏：** 否决按五维切块、整份一条向量、FAQ 三字段、只建库、五维全走向量、只保留一种 CLI、哈希/姓名当 ID、按哈希跳过嵌入、评估专家自己 search、空召回失败或回退全量。
- **翻车与返工：** 讨论未完就落盘 spec，按用户要求删除后改回口头对齐，四段批准后再写本文档。

## spec 修订（2026-09-10）

- **用户关键原话：** 「为什么要用完整json，而不是落库时存成单独字段？」；说明后回复「好，修改」。
- **关键产出：** 更新 spec：`resumes` 改为标量列（name / location / phone / email / highest_degree / experience_months），删除 `payload_json`；年限/学历/地点只读这些列；嵌套经历仍只在 `resume_chunks`。
- **拒绝或纠偏：** 否决父表持久化整份 Resume JSON 再反序列化打分；否决把工作/项目摊成宽表列。
- **翻车与返工：** 无。等待用户再审 spec。

## 计划评审通过（2026-09-10）

- **用户关键原话：** 「执行」（对修订后 spec 的批准）。
- **关键产出：** 实现计划 `docs/superpowers/plans/2026-09-10-resume-knowledge-base.md`。Context7 锁定：SQLAlchemy `create_async_engine("mysql+aiomysql://...")` + `async_sessionmaker`；PyMilvus `MilvusClient` + FLAT/COSINE + dim=1024；FlagEmbedding `BGEM3FlagModel.encode(...)["dense_vecs"]`。
- **拒绝或纠偏：** 无新否决。live 缺 KB 配置失败不 skip；日期算法抽到 `profile.py`，避免与打分两套逻辑。
- **翻车与返工：** 无。用户未单独再审计划正文，用「执行」跳过二次确认。

## Task 1–9 完成（2026-09-10）

- **用户关键原话：** 「1」（Subagent-Driven）。
- **关键产出：** `feat/ch03-rag` 上 8c7e1aa..bbbd98f：知识库模型/切块/标量打分/假存储 ingest/检索/MySQL+Milvus+BGE/ingest CLI/匹配双入口/live 评估。终审修学历 null=3、评估 prompt 参数名、ingest failed 覆盖、匹配复用 KB 句柄。
- **拒绝或纠偏：** 无新需求否决。
- **翻车与返工：** Task 2 学历映射重复；Task 3 误提交节点日志；Task 6 VARCHAR 无长度；Task 8 误提交 `.superpowers` 报告。均已修。live 因本机缺 MySQL/Milvus/BGE 按规格 FAIL。

