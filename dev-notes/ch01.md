# 第 1 章开发过程

## brainstorm 定稿（2026-09-09）

- **用户关键原话：** 「第一步先跑通一个最基本简历解析的流程」；入口选 C（库 + CLI）；语种选 A（只中文 Markdown）；字段名选 A（英文 key）；测试简历 `/Users/yannic/面试/宗艳云简历v9.md` 并给出 `personal_info` / `education` / `work_experience` / `skills` / `summary` 样例 JSON；项目经历选 B（在 JSON 上增加 `projects[]`）；流水线选方案 1（Markdown 标题规则切分 → 一次结构化提取 → Python 规则后处理）；架构/模块/切分与 Prompt/验证四段设计均回复「可以」。
- **关键产出：** 设计 spec `docs/superpowers/specs/2026-09-09-resume-parse-design.md`；范围锁定为第 1 章解析，不含岗位分析/匹配/打分、工具与 Agent、OCR。
- **拒绝或纠偏：** 用户用具体 JSON 收窄了最初「证书、多种字段」清单；否决「项目塞进 work_experience」和「项目再拆 background/methods」；同意简历原文不进 git。过程中未推翻已选的 C/A/A/B/方案1。
- **翻车与返工：** 无。空仓库绿地；样例 JSON 与真实简历结构不一致（缺 `projects`）已在提问中对齐，未写成错误 spec。

## 计划评审通过（2026-09-09）

- **用户关键原话：** 「执行」（对 spec 评审的回复，视为批准 spec 并继续 Superpowers 下一步）。
- **关键产出：** 实现计划 `docs/superpowers/plans/2026-09-09-resume-parse.md`；LangChain API 已用 Context7 核对（`ChatPromptTemplate.from_messages`、`ChatOpenAI(base_url/api_key/model)`、`with_structured_output(..., method="json_schema")`）。
- **拒绝或纠偏：** 无新的方案否决。计划按已锁定的方案 1 拆成 8 个任务；Prompt/提取用评估集代替假 LLM 单测。
- **翻车与返工：** 无。用户未单独再审计划正文，用「执行」跳过二次确认。

## Task 1 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `pyproject.toml`、`.gitignore`、`.env.example`、`job_agent/resume/schema.py`、`tests/test_schema.py`；`pytest tests/test_schema.py` 1 passed。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。RED 阶段按预期 `ModuleNotFoundError: job_agent`。

## Task 2 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `job_agent/resume/loader.py`、`tests/test_loader.py`；4 passed。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。RED 为 `No module named 'job_agent.resume.loader'`。
