# 第 1 章开发过程

## brainstorm 定稿（2026-09-09）

- **用户关键原话：** 「第一步先跑通一个最基本简历解析的流程」；入口选 C（库 + CLI）；语种选 A（只中文 Markdown）；字段名选 A（英文 key）；测试简历 `/Users/yannic/面试/宗艳云简历v9.md` 并给出 `personal_info` / `education` / `work_experience` / `skills` / `summary` 样例 JSON；项目经历选 B（在 JSON 上增加 `projects[]`）；流水线选方案 1（Markdown 标题规则切分 → 一次结构化提取 → Python 规则后处理）；架构/模块/切分与 Prompt/验证四段设计均回复「可以」。
- **关键产出：** 设计 spec `docs/superpowers/specs/2026-09-09-resume-parse-design.md`；范围锁定为第 1 章解析，不含岗位分析/匹配/打分、工具与 Agent、OCR。
- **拒绝或纠偏：** 用户用具体 JSON 收窄了最初「证书、多种字段」清单；否决「项目塞进 work_experience」和「项目再拆 background/methods」；同意简历原文不进 git。过程中未推翻已选的 C/A/A/B/方案1。
- **翻车与返工：** 无。空仓库绿地；样例 JSON 与真实简历结构不一致（缺 `projects`）已在提问中对齐，未写成错误 spec。

## 计划评审通过（2026-09-09）

- **用户关键原话：** 「执行」（对 spec 评审的回复，视为批准 spec 并继续 Superpowers 下一步）。
- **关键产出：** 实现计划 `docs/superpowers/plans/2026-09-09-resume-parse.md`；LangChain API 已用 Context7 核对（`ChatPromptTemplate.from_messages`、`ChatOpenAI(base_url/api_key/model)`、`with_structured_output(..., method="json_schema")`）。
- **拒绝或纠偏：** 无新的方案否决。计划按已锁定的方案 1 拆成 8 个任务；Prompt/提取用评估集代替假 LLM 单测。
- **翻车与返工：** 无。用户未单独再审计划正文，用「执行」跳过二次确认。

## Task 1 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `pyproject.toml`、`.gitignore`、`.env.example`、`job_agent/resume/schema.py`、`tests/test_schema.py`；`pytest tests/test_schema.py` 1 passed。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。RED 阶段按预期 `ModuleNotFoundError: job_agent`。

## Task 2 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `job_agent/resume/loader.py`、`tests/test_loader.py`；4 passed。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。RED 为 `No module named 'job_agent.resume.loader'`。

## Task 3 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `job_agent/resume/splitter.py`、`tests/test_splitter.py`；3 passed。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。

## Task 4 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `job_agent/resume/normalizer.py`、`tests/test_normalizer.py`；2 passed。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。

## Task 5 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `job_agent/config.py`、`tests/test_config.py`；2 passed。测试 stub `load_dotenv`，避免本机 `.env` 污染缺变量用例。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。

## Task 6 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `job_agent/resume/prompts.py`、`job_agent/resume/extractor.py`。Context7 复核 `with_structured_output(..., method="json_schema")` 仍有效。Prompt 变量 smoke-check 通过。
- **拒绝或纠偏：** 无。未改供应商 SDK。
- **翻车与返工：** 无。

## Task 7 完成（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** `job_agent/resume/pipeline.py`、`job_agent/parse.py`；无参数 CLI 退出码 2。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 无。

## Task 8 阻塞（2026-09-09）

- **用户关键原话：** 「执行」
- **关键产出：** gold JSON 与 `tests/eval_resume_parse.py`；确定性单测 12 passed。真简历评估未跑。
- **拒绝或纠偏：** 缺 `.env` 时评估 skip，避免默认 pytest 被环境拖红。
- **翻车与返工：** 仓库没有 `.env`，`OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL` 均未设置，验收 JSON 演示卡住。

## code review 结论（2026-09-09）

- **用户关键原话：** 「执行」（未另发审查指令；按 Superpowers 在任务完成后自动审查）
- **关键产出：** [Review](e10c1ae6-515b-41a5-b25c-af2822d3e5cf) 评估 Needs changes。已修：日期区间解析、gold 去掉真实手机号、eval 改为 `@pytest.mark.live` 且缺配置不再 skip、补硬字段断言。审查还发现 `resume/__init__.py` 导出 `parse_resume` 造成循环导入，已改为空包。
- **拒绝或纠偏：** 未接受「缺 .env 就 skip 算完成」。未改供应商 SDK。
- **翻车与返工：** live 评估仍缺 `.env`，第 1 章验收 JSON 尚未跑通。

## Task 8 完成（2026-09-09）

- **用户关键原话：** 「填好了」
- **关键产出：** `pytest -m live tests/eval_resume_parse.py` 1 passed（约 101s）；`python -m pytest` 16 passed；CLI 抽出姓名/教育/三段工作履历/项目 JSON。
- **拒绝或纠偏：** 未换 SDK。DeepSeek 不支持 `json_schema`，按文档改用 `with_structured_output(..., method="json_mode")`，并加大 `max_tokens`。
- **翻车与返工：** 第一次 live 因 `json_schema` 400 失败；`json_mode` 一次抽空教育、一次把职责抽成字符串。补了标题区姓名回填、列表字段兼容校验后重跑通过。

## finish（2026-09-09）

- **用户关键原话：** 「填好了」（补齐 `.env` 后继续验收）
- **关键产出：** 第 1 章解析链路验收通过。过程笔记本文件。
- **拒绝或纠偏：** 无。
- **翻车与返工：** 见 Task 8。

## 验收返工：education 单对象（2026-09-09）

- **用户关键原话：** 指向 CLI 报错 `education Input should be a valid list ... input_type=dict`
- **关键产出：** `Resume` 对 `education` / `work_experience` / `projects` 把单个对象包成数组；Prompt 要求这些字段即使一条也必须是数组。
- **拒绝或纠偏：** 未改 schema 契约（仍是 `list[Education]`），只做 LLM 输出形态兼容。
- **翻车与返工：** `json_mode` 在只有一条教育经历时输出对象而非数组，与之前职责被抽成字符串同类。

## 增量：target_role 求职岗位（2026-09-10）

- **用户关键原话：** 「简历提取再加一个求职岗位」
- **关键产出：** `Resume.target_role`；切分映射求职意向/方向/岗位；Prompt 增加求职岗位块；规则从「求职方向：」行回填；gold 为样例「AI Agent / AI 应用工程化 / Java 全栈开发」。
- **拒绝或纠偏：** 字段放在 Resume 顶层，不塞进 `personal_info`。
- **翻车与返工：** 无。
